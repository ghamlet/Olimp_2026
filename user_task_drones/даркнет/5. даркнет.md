Darknet это фраемворк для обучения нейронных сетей. Нам необходимо самостоятельно скомпилировать её из исходного кода.

Следующие блоки кода скопируют исходный код darknet из репозитория AlexeyAB в наш Colab, отредактируют Makefile так, чтобы использовать OPENCV (в настоящее время опенсисви убрали из установленных пакетов) и GPU для darknet и затем скомпилируют darknet.


> [!NOTE]
> 
> Ошибка в том, что в Colab нет dev-пакета OpenCV (заголовков + pkg-config). README требует OpenCV при OPENCV=1. Установи его перед !make:
> !apt-get update
> !apt-get install -y libopencv-dev
> !make
> Если OpenCV тебе не нужен для тренировки (нужен только для видео/вывода картинок самим darknet'ом) — можно просто вернуть OPENCV=0 и собрать без него:
> !sed -i 's/OPENCV=1/OPENCV=0/' Makefile



дальше по блокноту


проверить еслть ли разница в скорости работы 
1. !git clone https://github.com/AlexeyAB/darknet   в сам блокнот
2. !git clone https://github.com/AlexeyAB/darknet на гугл диск, но так ты теряешь кфг и неймс после перезагрузки блокнота, но это можно пофиксить если сразу скачать эти файлы себе на диск



  
  

**СОВЕТ.** Обучение может занять несколько часов в зависимости от того, сколько итераций (max_batches) вы выбрали в файле .cfg. Возможно, вы захотите отойти от ПК, однако облачная служба Colab отключает вас от своих виртуальных машин, если вы бездействуете слишком долго (30-90 минут).

  
Чтобы это избежать нажмите `CTRL + SHIFT + i`, чтобы открыть консоль браузера.

  
Вставьте следующий код в консоль и нажмите **Enter**.

```

function ClickConnect(){

console.log("Working");

document

.querySelector('#top-toolbar > colab-connect-button')

.shadowRoot.querySelector('#connect')

.click()

}

ClickConnect();

const interval_id = setInterval(ClickConnect, 60*1000);

```

  

Этот скрит будет нажимать на экран каждую минуту, поэтому вас не отключат. НО НЕ ФАКТ! Google периодически меняют свои проверочные системы и это может не работать.

  

Чтобы отключить этот скрипт, вставьте в консоль и запустите это:

```

clearInterval(interval_id);

```

Скрипт перестанет кликать на экран




Продолжить обучение, если оборвалось
Не забудьте поменять имя весов

```
!./darknet detector train data/obj.data cfg/yolov4-obj-tiny.cfg <укажите имя весов!> -dont_show -map
```



типа всем следует использовать auto_train 


дописать про использование  полученных моделей через питон

запуск скрипта **auto_train.ipynb** в рабочей папке которую вы указали внутри блокнота
как **рабочую папку** внутри вашего гугл диска. 
В данном случае WORK_DIR = "Olimp_2026"
![[Pasted image 20260826144820.png]]


на вашем гугл диске в рабочей директории WORK_DIR будет автоматически создана  папка с названием backup_%Y-%m-%d_%H-%M-%S


![[Pasted image 20260826144617.png]]


В ней сразу будут сохранены файлы:
1. yolov4-tiny-obj.cfg
2. obj.names

которые будут использоваться в дальнейшем при инференсе(запуске) модели в программном коде. 
![[Pasted image 20260826145214.png]]

Затем спустя некоторое время в данной папке начнут сохраняться промежуточные веса
![[Pasted image 20260826145409.png]]


Данная система с бэкапами сделана для того, чтобы:
	1. ситуация, когда вы решили поменть некоторые параметры в начале скрипта и решили запустить скрипт сначала 
	2. 